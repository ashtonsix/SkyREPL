================================================================================
11. AGENT
================================================================================

## Purpose

The Agent Protocol defines communication between the control plane and Python
agents running on remote instances. The agent is intentionally simple: stdlib-only
Python that receives commands, executes runs, streams logs, and reports status.
Complexity lives in the control plane, not the agent.


## 11.1 Communication Architecture

Three distinct channels, all agent-initiated:

```
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                          CONTROL PLANE                                  │
    └───────────────────────────────┬─────────────────────────────────────────┘
                                    │
          ┌─────────────────────────┼─────────────────────────┐
          │                         │                         │
          │ SSE Stream              │ HTTP POST               │ Presigned URLs
          │ (commands)              │ (logs/heartbeat)        │ (artifacts)
          │                         │                         │
          ▼                         ▲                         ↕
    ┌─────────────────────────────────────────────────────────────────────────┐
    │                              AGENT                                      │
    ├─────────────────────────────────────────────────────────────────────────┤
    │  Threads:                                                               │
    │    • Command handler  ─ SSE consumer, receives commands                 │
    │    • Heartbeat        ─ 10s interval, liveness signal                   │
    │    • Log flusher      ─ 100ms local buffer, 5s network batch           │
    │    • Spot monitor     ─ 5s interval, AWS only                           │
    │    • Run execution    ─ spawned per run                                 │
    └─────────────────────────────────────────────────────────────────────────┘

    ╭─────────────────────────────────────────────────────────────────────────╮
    │  LEGEND:  ▼ control→agent    ▲ agent→control    ↕ bidirectional        │
    ╰─────────────────────────────────────────────────────────────────────────╯
```

> Why agent-initiated rationale extracted to docs/design/rationale-execution.txt

### Channel Purposes

**SSE Stream (control -> agent)**: Command delivery. Single long-lived connection;
agent reads byte-by-byte to avoid buffer deadlock (see 11.6).

**HTTP POST (agent -> control)**: Status reporting. Two-level log batching: local
buffer (stdout/stderr collection), network batch on interval OR size threshold.
Control plane reorders batches via batch_sequence_id. Heartbeats at configured
interval. Fire-and-forget with retry. Intervals configurable (see Ch15 §15.9).

**Presigned URLs**: Artifact upload/download bypasses control plane via direct
S3 PUT/GET (or control plane endpoint for small objects <=64KB - see 11.11).

## 11.2 Schema Notes

**allocation_id** in run messages: Tracks which allocation executes which run,
enabling resource management and SSH session tracking. See Ch5 for allocation
lifecycle.

**workflow_state** in RunCompleteMessage: Uses workflow state format
(e.g., 'launch-run:complete') instead of boolean, enabling richer state
information. See Ch6 for state format.

**workflow_state** in HeartbeatMessage: Control plane knows instance lifecycle
phase without polling.

**active_allocations**: Array of {allocation_id, has_ssh_sessions} objects
reporting per-allocation SSH state. Control plane uses this for per-allocation
debug hold extension (Ch5). Replaces the previous active_ssh_sessions count.

## 11.3 Message Types

### Control -> Agent (SSE Stream)

| Type              | Purpose                                    |
|-------------------|-------------------------------------------|
| start_run         | Begin run with file sync and execution    |
| cancel_run        | Terminate running run                     |
| capture_artifacts | Upload artifacts (mid-run or emergency)   |
| prepare_snapshot  | Prepare instance for snapshot creation    |
| trigger_tailscale | JIT Tailscale installation                |
| heartbeat_ack     | Acknowledge agent heartbeat               |

### Agent -> Control (HTTP POST)

| Type                   | Purpose                                    |
|------------------------|-------------------------------------------|
| log                    | Stdout/stderr chunk or sync_complete      |
| artifact               | Artifact upload confirmation              |
| init_complete          | Init script finished successfully         |
| run_complete           | Run finished with exit code               |
| prepare_snapshot_ack   | Acknowledge snapshot preparation complete |
| spot_interrupt_start   | AWS spot interruption detected, starting checkpoint |
| spot_interrupt_complete| Checkpoint done, artifacts uploaded       |
| heartbeat              | Liveness signal with status               |

## 11.4 Message Schemas

### Control -> Agent Messages

```typescript
// NOTE: IDs are integers internally, transmitted as numbers in JSON.

// SSE: start_run (combines file download + run execution)
interface StartRunMessage {
  type: 'start_run';
  run_id: string;             // Globally unique run identifier
  allocation_id: number;      // Which allocation this run belongs to
  command_id: number;         // Unique ID for command deduplication (see 11.4.5)
  command: string;
  workdir: string;
  files: Array<{
    path: string;             // Relative path in workdir
    checksum: string;         // SHA256 checksum for verification
    url: string;              // Presigned URL (S3 or control plane)
  }>;
  artifacts?: string[];       // Glob patterns to collect
  run_init?: boolean;         // Execute init script first
  init_script?: string;       // Init script content
  create_snapshot?: boolean;  // Snapshot-only mode
}

// SSE: cancel_run
interface CancelRunMessage {
  type: 'cancel_run';
  run_id: string;
  command_id: number;         // Unique ID for command deduplication (see 11.4.5)
}

// SSE: capture_artifacts
interface CaptureArtifactsMessage {
  type: 'capture_artifacts';
  run_id: string;
  command_id: number;         // Unique ID for command deduplication (see 11.4.5)
}

// SSE: prepare_snapshot
interface PrepareSnapshotMessage {
  type: 'prepare_snapshot';
  instance_id: number;        // Instance to prepare for snapshot
  command_id: number;         // Unique ID for command deduplication (see 11.4.5)
}

// SSE: trigger_tailscale
interface TriggerTailscaleMessage {
  type: 'trigger_tailscale';
}

// SSE: heartbeat_ack
interface HeartbeatAckMessage {
  type: 'heartbeat_ack';
  control_plane_id: string;  // Unique control plane identifier (agent validates consistency)
}
```

### Agent -> Control Messages

```typescript
// POST: log (batched, includes sync_complete)
interface LogMessage {
  type: 'log';
  run_id: string;
  allocation_id: number;          // Which allocation this run belongs to
  batch_sequence_id: number;      // Monotonic counter per agent session for reordering
  stream: 'stdout' | 'stderr' | 'sync_complete';
  data: string;
  phase?: 'sync' | 'execution';   // Distinguishes sync failure from run failure
  sync_success?: boolean;          // Only for sync_complete
}

// POST: artifact
interface ArtifactMessage {
  type: 'artifact';
  run_id: string;
  path: string;
  s3_key: string;
  size_bytes: number;
}

// POST: init_complete
interface InitCompleteMessage {
  type: 'init_complete';
  run_id: string;
}

// POST: run_complete
interface RunCompleteMessage {
  type: 'run_complete';
  run_id: string;
  allocation_id: number;      // Which allocation this run belongs to
  exit_code: number;          // Process exit code (0 = success)
  workflow_state: string;     // Target state: 'launch-run:complete' or 'launch-run:error'
                              // (see Chapter 6 for workflow state format)
  interrupted?: boolean;      // True if spot interruption
}

// POST: prepare_snapshot_ack
interface PrepareSnapshotAckMessage {
  type: 'prepare_snapshot_ack';
  instance_id: number;        // Instance that was prepared
  success: boolean;           // Whether preparation succeeded
  error_message?: string;     // Error details if success=false
}

// POST: spot_interrupt (two-phase protocol)
interface SpotInterruptStartMessage {
  type: 'spot_interrupt_start';
  run_id: string;
  time_remaining_ms: number;
}

interface SpotInterruptCompleteMessage {
  type: 'spot_interrupt_complete';
  run_id: string;
  checkpoint_success: boolean;
  artifacts_uploaded: number;
}

// POST: heartbeat
interface HeartbeatMessage {
  tailscale_ip: string | null;
  active_allocations: Array<{    // Per-allocation SSH state for debug hold management (see Ch5).
    allocation_id: number;        // Allocation being served by this agent.
    has_ssh_sessions: boolean;    // True if allocation has active SSH sessions.
  }>;
  tailscale_status: 'not_installed' | 'installing' | 'ready' | 'failed';
  workflow_state: string;         // Instance lifecycle state (e.g., 'launch-run:sync',
                                  // 'launch-run:running', 'idle'). See Chapter 6 for format.
  pending_command_acks: number[]; // Command IDs awaiting acknowledgment (see 11.4.5)
  dropped_logs_count: number;     // Tracks log buffer overflow events for operational visibility
}
```

## 11.4.5 Command Acknowledgment Protocol

SSE is lossy. Without acknowledgment, command loss causes silent failures (runs never
start, snapshots never captured). This protocol ensures reliable delivery.

**Protocol:**
1. Control plane sends command with unique `command_id`
2. Agent receives command, queues ack, processes command
3. Agent includes `pending_command_acks` in next heartbeat
4. Control plane marks acknowledged, removes from send queue
5. Control plane retries unacknowledged commands after COMMAND_ACK_TIMEOUT (max 3 retries)

**Deduplication**: Agent tracks last 100 `processed_command_ids` to ignore retries.

**Type signatures:**
```typescript
interface AgentCommandState {
  pending_command_acks: Set<number>;    // Commands awaiting ack transmission
  processed_command_ids: Set<number>;   // Last 100 command IDs (dedup)
}

interface CommandDeliveryState {
  command_id: number;
  sent_at: number;
  retry_count: number;
  acknowledged: boolean;
}
```

**Configuration:**
- `COMMAND_ACK_TIMEOUT_MS` = 30000 (30s, configurable)
- `MAX_COMMAND_RETRIES` = 3
- `PROCESSED_COMMAND_HISTORY_SIZE` = 100 (fixed)

**Edge Cases:**
- Agent restart: may re-execute recent commands (idempotent handlers)
- Control plane restart: pending commands reloaded from `command_delivery` table
- Network partition: commands queue on control side, delivered when reconnected

## 11.5 Agent Lifecycle

```
╔══════════════════════════════════════════════════════════════════════════════╗
║                            AGENT LIFECYCLE                                   ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│  STARTUP                                                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. Acquire lock (/tmp/repl-agent.lock)                                      │
│     └─ Prevents duplicate agents on warm pool reuse                          │
│     └─ Exit immediately if lock held                                         │
│                                                                              │
│  2. Detect Tailscale IP (if available)                                       │
│     └─ Check tailscale0 interface                                            │
│     └─ Check tailscale CLI                                                   │
│                                                                              │
│  3. Start background threads                                                 │
│     ├─ Log flusher   (100ms interval, configurable)                          │
│     ├─ Heartbeat     (10s interval, IMMEDIATE first, configurable)           │
│     └─ Spot monitor  (5s interval, AWS only, configurable)                   │
│                                                                              │
│  4. Connect SSE command stream                                               │
│     └─ Retry with exponential backoff (max 10 retries)                       │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  RUNNING                                                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────┐   ┌────────────────┐   ┌────────────────┐                │
│  │  SSE Commands  │   │  Run Executor  │   │   Heartbeat    │                │
│  │    (recv)      │──▶│   (per run)    │   │    (10s)       │                │
│  └────────────────┘   └───────┬────────┘   └────────────────┘                │
│                               │                                              │
│                               ▼                                              │
│                       ┌────────────────┐   ┌────────────────┐                │
│                       │  Log Flusher   │   │  Spot Monitor  │                │
│                       │   (100ms)      │   │  (5s, AWS)     │                │
│                       └────────────────┘   └────────────────┘                │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
                                       │
             ┌─────────────────────────┼─────────────────────────┐
             │                         │                         │
      Heartbeat timeout         Connection lost           SIGTERM/SIGINT
       (15m no ack)            (10 retries fail)             received
             │                         │                         │
             └─────────────────────────┼─────────────────────────┘
                                       │
                            ┌──────────┴──────────┐
                            │  Spot Interruption  │
                            │   (emergency path)  │
                            └──────────┬──────────┘
                                       │
                                       ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  SHUTDOWN                                                                    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  1. Set shutting_down flag                                                   │
│                    │                                                         │
│                    ▼                                                         │
│  2. Tailscale logout (cleanup device list)                                   │
│                    │                                                         │
│                    ▼                                                         │
│  3. Terminate running run (if any)                                           │
│                    │                                                         │
│                    ▼                                                         │
│  4. Flush remaining logs                                                     │
│                    │                                                         │
│                    ▼                                                         │
│  5. Wait grace period (SHUTDOWN_GRACE_PERIOD)                                │
│                    │                                                         │
│                    ▼                                                         │
│  6. Self-terminate: shutdown -h now (BACKUP SAFETY)                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘
```

### Immediate Initial Heartbeat

Heartbeat thread sends a heartbeat BEFORE its first sleep to close the timing
gap where control plane might terminate for "no heartbeat" before agent reports
ready. Control plane should also allow grace period during Tailscale setup.

```python
def heartbeat_thread():
    if not send_heartbeat():  # Immediate on startup
        log("WARN", "Initial heartbeat failed")

    while not shutting_down:
        time.sleep(HEARTBEAT_INTERVAL_MS / 1000.0)  # Default: 10s
        # ... rest of heartbeat logic
```

### Self-Termination is BACKUP Safety

Agent's `shutdown -h now` is LAST RESORT. Normal path: control plane terminates
via provider API. Backup: agent self-terminates if control plane unreachable
(crash, network partition, bug). Agent doesn't rely on control plane for
termination - shuts down after heartbeat timeout regardless.

### Degraded State and Graceful Panic

When control plane becomes unreachable, agent progresses through degraded and
panic states before self-terminating:

```
Timeline (from last successful heartbeat ack):
t=0        Last ack received
t=2m       Enter DEGRADED state (workflow_state = 'degraded:control_plane_unreachable')
           - Continue heartbeating (control plane may recover)
           - Provides early warning visibility to CLI users
t=15m      Enter PANIC state, initiate graceful shutdown:
           1. Send HeartbeatPanicStartMessage (best-effort, may fail if CP unreachable)
           2. Execute checkpoint script (PANIC_CHECKPOINT_BUDGET_MS = 5m)
           3. Upload artifacts with failover URLs
           4. Send HeartbeatPanicCompleteMessage (best-effort)
           5. Initiate shutdown sequence
t=20m      Expected self-termination complete
t=25m      Control plane force-terminates (if agent failed to self-term)
```

If control plane recovers during degraded window (2m-15m), agent receives ack,
clears degraded state, and resumes normal operation. This prevents unnecessary
panic checkpoints for transient network issues.

The panic checkpoint path reuses spot interruption logic (see §11.5 Spot Monitor)
with different timeout budget: 5m for panic vs 90s for spot. Environment variable
`SKYREPL_CHECKPOINT_REASON` is set to `heartbeat_timeout` (vs `spot_termination`).

## 11.6 SSE Implementation Details

### Byte-by-Byte Reading

Agent MUST use `read(1)` syscall to bypass Python HTTP library buffering.

> Byte-by-byte reading rationale and pseudocode extracted to docs/design/rationale-execution.txt

> See also: impl/agent/sse.py.l2.txt §LowLevelByteReading

### Connection Management

Agent MUST:
1. Connect to `/v1/agent/commands` with `Accept: text/event-stream` header
2. Check response status (200 OK required)
3. Use read_lines() to consume SSE stream
4. Parse each line as JSON command message
5. On error: log and reconnect with exponential backoff (2s initial, 64s max, 10 retries)

> See also: impl/agent/sse.py.l2.txt §ConnectionManagement

## 11.7 Agent Configuration

### Timing Constants

Configurable via environment variables (cloud-init user data). Constraints:
HEARTBEAT_INTERVAL_MS < HEARTBEAT_DEGRADED_MS < HEARTBEAT_ACK_TIMEOUT_MS < STALE_DETECTION_MS
(control plane, see Ch15). RECONNECT_BASE < RECONNECT_MAX.

| Constant                | Default | Env Variable                  | Purpose                       |
|-------------------------|---------|-------------------------------|-------------------------------|
| HEARTBEAT_INTERVAL_MS   | 10000   | REPL_HEARTBEAT_INTERVAL_MS    | Agent -> control heartbeat    |
| HEARTBEAT_DEGRADED_MS   | 120000  | REPL_HEARTBEAT_DEGRADED_MS    | Degraded state threshold (2m) |
| HEARTBEAT_ACK_TIMEOUT_MS | 900000  | REPL_HEARTBEAT_ACK_TIMEOUT    | Panic/shutdown trigger (15m)  |
| PANIC_CHECKPOINT_BUDGET_MS | 300000  | REPL_PANIC_CHECKPOINT_BUDGET  | Checkpoint budget during panic|
| LOG_LOCAL_BUFFER_MS     | 100     | REPL_LOG_LOCAL_BUFFER         | Batched log upload            |
| SPOT_CHECK_INTERVAL_MS  | 5000    | REPL_SPOT_CHECK_INTERVAL      | AWS metadata polling          |
| AGENT_SHUTDOWN_GRACE_MS | 5000    | REPL_AGENT_SHUTDOWN_GRACE     | Delay before self-termination (5s) |
| SSE_RECONNECT_BASE_S    | 2       | REPL_SSE_RECONNECT_BASE_S     | Initial reconnect delay       |
| SSE_RECONNECT_MAX_S     | 64      | REPL_SSE_RECONNECT_MAX_S      | Maximum reconnect delay       |
| SSE_MAX_RETRIES         | 10      | REPL_SSE_MAX_RETRIES          | Reconnects before shutdown    |

**Heartbeat Timeout Constraint:** Agent self-terminates after 15m without heartbeat
acknowledgment (HEARTBEAT_ACK_TIMEOUT_MS = 900000). Control plane force-terminates
instances after 25m without heartbeat (see Ch15 §15.9 for STALE_DETECTION_MS).
This 10-minute gap ensures agent-initiated cleanup completes before provider-initiated
termination, reducing false positives for long-running jobs while maintaining safety.

### When to Adjust

| Adjustment                 | Use Case                 | Trade-off                      |
|----------------------------|--------------------------|--------------------------------|
| Longer heartbeat (20-30s)  | Stable, long-running     | Slower dead instance detection |
| Shorter heartbeat (5s)     | High-churn ephemeral     | Higher control plane load      |
| Longer log flush (500ms-1s)| Heavy logging            | Delayed UI visibility          |
| Shorter spot check (2-3s)  | Critical spot workloads  | Higher metadata API load       |
| Shorter heartbeat timeout  | Dev/test, fast failure   | More false positives, less     |
| (30s-60s)                  | detection needed         | time for graceful checkpoint   |
| Shorter degraded threshold | Earlier UX warning       | More noise for brief hiccups   |
| (30s-60s)                  |                          |                                |

### Example: User Data Configuration

```yaml
#cloud-config
write_files:
  - path: /etc/environment
    content: |
      REPL_HEARTBEAT_INTERVAL_MS=15000
      REPL_HEARTBEAT_ACK_TIMEOUT=45000
    append: true
```

## 11.8 Lock Acquisition

Agent MUST acquire exclusive lock `/tmp/repl-agent.lock` on startup to prevent duplicate agents on warm pool reuse. Use `fcntl.flock(LOCK_EX | LOCK_NB)` for non-blocking exclusive lock. If lock fails, agent MUST exit immediately with FATAL log.

> See also: impl/agent/agent.py.l2.txt §LockAcquisition

## 11.9 Spot Interruption Handling

**Detection Protocol (AWS only, two-phase):**

**Phase 1: Detect Interruption (IMDSv2)**
```
FUNCTION check_spot_interruption():
  IF provider != "aws":
    RETURN null

  // Step 1: Acquire IMDSv2 token
  token = HTTP_PUT("http://169.254.169.254/latest/api/token",
                   headers={"X-aws-ec2-metadata-token-ttl-seconds": "21600"},
                   timeout=2s)

  // Step 2: Check for interruption notice
  TRY:
    response = HTTP_GET("http://169.254.169.254/latest/meta-data/spot/instance-action",
                        headers={"X-aws-ec2-metadata-token": token},
                        timeout=2s)
    RETURN parse_json(response)
  CATCH HTTPError(404):
    RETURN null  // No interruption pending
  CATCH any_error:
    RETURN null  // Transient failure, retry next poll
```

**Phase 2: Response Protocol (Two-Phase)**
1. Set `spot_interrupted` flag
2. Send `spot_interrupt_start` message (interrupt detected, starting checkpoint)
3. Execute checkpoint script if configured (user-defined save logic)
4. Upload partial artifacts (best effort)
5. Send `spot_interrupt_complete` message (checkpoint done, artifacts uploaded)
6. Send `run_complete` with `interrupted: true`
7. Terminate run process

**Checkpoint Script Contract:**
- Path: Relative to repl.toml
- Timeout: Configurable via `checkpoint_timeout` (see Ch10)
- Environment: `SKYREPL_CHECKPOINT_REASON=spot_termination`, `SKYREPL_RUN_ID=run-xyz789`, `SKYREPL_WORKDIR=/path`
- Output: Streamed as regular logs
- Termination: Killed if exceeds timeout, then proceeds with artifact upload

**Polling Interval:** Configurable via REPL_SPOT_CHECK_INTERVAL (default 5s, see Ch15 §15.9). Balances responsiveness with metadata API rate limits.

## 11.10 Workdir Cleanup

Agent MUST clean workdir at start of `start_run` before downloading files. Critical for warm pool: previous run's secrets visible (security), stale files affect behavior (correctness), accumulated files fill disk.

**Protocol:** If workdir exists, change cwd to home directory, then recursively remove workdir. Log warning on failure but continue (best effort).

> See also: impl/agent/executor.py.l2.txt §WorkdirCleanup

## 11.11 Presigned URL Abstraction

Control plane MUST generate presigned URLs for all file operations. Agent uses identical HTTP PUT/GET regardless of backing store:
- **>64KB**: URL points to S3 directly (control plane never sees payload)
- **<=64KB**: URL points to control plane endpoint (avoids S3 round-trip)

Agent implementation: `urllib.request.Request(url, data=file_data, method="PUT")`. See Ch2 for 64KB threshold rationale.

> See also: impl/agent/artifacts.py.l2.txt §ArtifactUpload

## 11.12 JIT Tailscale Installation

Tailscale MUST be installed on-demand (when SSH requested or instance enters warm pool) using static binary (faster than apt, cross-distro, no lock contention).

**Benefits:** Faster startup (no bootstrap install), overhead only when SSH needed, auth key injected at install time (no baked secrets).

**Protocol:** Check existing status → detect existing IP → if not found, download static binary → start daemon → authenticate.

> See also: impl/agent/tailscale.py.l2.txt §JITInstallation

## 11.13 Error Handling Philosophy

**Transient (retry with backoff):** Network timeout, SSE dropped, presigned URL
expired.

**Fatal (clean shutdown):** Missing env vars, lock failed, heartbeat timeout,
max retries exhausted.

No complex recovery. If fundamentally wrong, terminate and let control plane
spawn fresh instance rather than limp along degraded.

## 11.14 Failsafe Self-Termination Diagnostics

Agent SHOULD send best-effort diagnostics to `/v1/instances/:id/panic` before shutdown (heartbeat timeout, SSE loss, fatal errors, signals, spot interruption).

**Payload Requirements:**
- `reason`: Shutdown trigger (e.g., "heartbeat_timeout", "spot_interrupt")
- `last_state`: workflow_state, run_id, allocation_id, uptime, last_heartbeat_ack, sse_reconnect_count
- `diagnostics`: CPU%, memory%, disk%, active_threads, log_buffer_size
- `error_logs`: Recent error lines (max 50)
- `timestamp`: Unix milliseconds

**Transmission Constraints:**
- Timeout: 2 seconds (MUST NOT block shutdown)
- Failures: Expected and acceptable (log warning, continue shutdown)
- Success rate target: 70-90% (supplements normal logging)

> See also: impl/agent/agent.py.l2.txt §PanicDiagnostics

**Shutdown Sequence:** Set flag → send diagnostics (2s timeout) → Tailscale logout → terminate run → flush logs → grace period → self-terminate.

