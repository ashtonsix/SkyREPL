# whitelist.ts - Orphan Whitelist Management (L2 Pseudocode)

## Imports

```typescript
import { queryMany, queryOne } from '../material/db';
```

## Types

```typescript
interface WhitelistEntry {
  provider: string;
  providerId: string;
  resourceType: string;
  resourceName?: string;
  reason: string;
  acknowledgedBy: string;
  acknowledgedAt: number;
  expiresAt?: number;
  notes?: string;
}
```

## Schema

```sql
CREATE TABLE orphan_whitelist (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    provider TEXT NOT NULL,
    provider_id TEXT NOT NULL,
    resource_type TEXT NOT NULL,
    resource_name TEXT,

    -- Why this orphan is being kept
    reason TEXT NOT NULL,

    -- Who made the decision
    acknowledged_by TEXT NOT NULL,
    acknowledged_at INTEGER NOT NULL,

    -- Optional expiry (re-evaluate after this time)
    expires_at INTEGER,

    -- Additional context
    notes TEXT,

    UNIQUE(provider, provider_id)
);

CREATE INDEX idx_whitelist_expires ON orphan_whitelist(expires_at);
CREATE INDEX idx_whitelist_provider ON orphan_whitelist(provider, resource_type);
```

## WhitelistOperations

```typescript
/**
 * Add or update an orphan whitelist entry.
 * Upsert pattern: if provider+providerId already exists, update fields.
 * id is auto-generated by database (INTEGER PRIMARY KEY AUTOINCREMENT).
 */
async function whitelistOrphan(entry: WhitelistEntry): Promise<void> {
  await queryOne(`
    INSERT INTO orphan_whitelist
    (provider, provider_id, resource_type, resource_name, reason,
     acknowledged_by, acknowledged_at, expires_at, notes)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?)
    ON CONFLICT(provider, provider_id) DO UPDATE SET
      reason = excluded.reason,
      acknowledged_by = excluded.acknowledged_by,
      acknowledged_at = excluded.acknowledged_at,
      expires_at = excluded.expires_at,
      notes = excluded.notes
  `, [
    entry.provider,
    entry.providerId,
    entry.resourceType,
    entry.resourceName,
    entry.reason,
    entry.acknowledgedBy,
    entry.acknowledgedAt,
    entry.expiresAt,
    entry.notes,
  ]);
}

/**
 * Get all non-expired whitelist entries.
 */
async function getActiveWhitelist(): Promise<WhitelistEntry[]> {
  return queryMany(`
    SELECT * FROM orphan_whitelist
    WHERE expires_at IS NULL OR expires_at > ?
  `, [Date.now()]);
}

/**
 * Remove a specific entry from the whitelist.
 */
async function removeFromWhitelist(provider: string, providerId: string): Promise<void> {
  await queryOne(`
    DELETE FROM orphan_whitelist WHERE provider = ? AND provider_id = ?
  `, [provider, providerId]);
}
```
