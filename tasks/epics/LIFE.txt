================================================================================
EPIC LIFE — Lifecycle Completeness
================================================================================
Spec: Ch03 (Resources), Ch04 (Manifest), Ch05 (Allocations), Ch07 (Intents)

Scope:
  The system can't clean up after itself without this. Fills resource
  stubs, wires background tasks, builds terminate-instance and
  cleanup-manifest intents.

Pseudo subsumed:
  resource/{allocation,instance,run,manifest}.ts.l1.txt
  background/{reconciliation,hold-expiry,garbage-collection,manifest-cleanup}.ts.l1.txt
  intent/{terminate,cleanup-manifest}.ts + types + node files

================================================================================
TASK INDEX
================================================================================

  #LIFE-01  Fill resource layer stubs                            [DONE]
  #LIFE-02  Heartbeat timeout enforcement                        [DONE]
  #LIFE-03  Hold expiry lifecycle                                [DONE]
  #LIFE-04  Manifest expiry scanner                              [DONE]
  #LIFE-05  Blob GC wiring                                       [DONE]
  #LIFE-06  DDL migration for cleanup-manifest schema            [DONE]
  #LIFE-07  terminate-instance intent (blueprint + nodes)        [DONE]
  #LIFE-08  terminate-instance API + CLI wiring                  [DONE]
  #LIFE-09  cleanup-manifest intent (blueprint + nodes)          [DONE]
  #LIFE-10  cleanup-manifest background trigger                  [DONE]
  #LIFE-11  Allocation reconciliation                            [DONE]
  #LIFE-12  Resource claiming protocol                           [DONE]
  #LIFE-13  Enforce resource layer boundary                      [PENDING]

================================================================================
TASK DETAIL
================================================================================

## #LIFE-01: Fill resource layer stubs                             [Lg] [DONE]
  Carries: SM-21
  Depends: #FND-01
  ---
  resource/allocation.ts, instance.ts, run.ts all throw "not implemented".
  Replace with actual materialization logic.
  Ref: impl-pseudo resource/{allocation,instance,run}.ts.l1.txt

  Deliverables:
  - [x] Allocation resource: CRUD, state queries, warm pool helpers
  - [x] Instance resource: lifecycle queries, provider state mapping
  - [x] Run resource: CRUD, exit code, completion tracking
  - [x] Unit tests for each resource module

## #LIFE-02: Heartbeat timeout enforcement                         [Md] [DONE]
  Depends: #FND-01
  Pseudo: control/src/background/reconciliation.ts.l1.txt
  ---
  Wire heartbeatTimeoutCheck background task (currently no-op in main.ts).
  Detect stalled agents, transition allocations to FAILED.

  Deliverables:
  - [x] Implement heartbeatTimeoutCheck in background scheduler
  - [x] Transition stalled allocations (no heartbeat > threshold)
  - [x] Integration test: simulate stalled heartbeat → allocation fails

## #LIFE-03: Hold expiry lifecycle                                 [Md] [DONE]
  Depends: #FND-01
  Spec: Ch05 §5.7
  Pseudo: control/src/background/hold-expiry.ts.l1.txt
  ---
  Wire holdExpiryCheck background task. Debug holds with automatic expiry,
  max 24h. Atomic update pattern.

  Deliverables:
  - [x] Implement holdExpiryCheck
  - [x] Atomic transition: held → released on expiry
  - [x] Extend API support (hold extension from CLI)

## #LIFE-04: Manifest expiry scanner                               [Md] [DONE]
  Depends: #FND-01
  Pseudo: control/src/background/manifest-cleanup.ts.l1.txt
  ---
  Wire manifestCleanupCheck background task. Triggers cleanup-manifest
  intent for expired manifests.

  Deliverables:
  - [x] Implement manifestCleanupCheck
  - [x] Scan for sealed manifests past retention period
  - [ ] Queue cleanup-manifest intent for each (deferred to #LIFE-09)

## #LIFE-05: Blob GC wiring                                        [Md] [DONE]
  Depends: #FND-01
  Pseudo: control/src/background/garbage-collection.ts.l1.txt
  ---
  Wire storageGarbageCollection background task. Object expiry,
  idempotency key expiry, dedup merger.

  Deliverables:
  - [x] Implement storageGarbageCollection
  - [x] Object expiry (unreferenced blobs past TTL)
  - [x] Idempotency key expiry

## #LIFE-06: DDL migration for cleanup-manifest schema             [Md] [DONE]
  Carries: BT1
  Depends: #FND-01
  ---
  5 DDL schema issues blocking cleanup-manifest intent. Fix migration.

  Deliverables:
  - [x] New migration fixing schema mismatches
  - [x] Verify cleanup-manifest tables match spec Ch04

## #LIFE-07: terminate-instance intent (blueprint + nodes)         [Xl] [DONE]
  Spec: Ch07 §7.2
  Pseudo: intent/terminate.ts.l1.txt, terminate.types.ts.l1.txt, related nodes
  Depends: #LIFE-01, #WF-01
  ---
  6-node DAG: validate-instance → drain-allocations → drain-ssh-sessions →
  cleanup-features → terminate-provider → cleanup-records.

  Deliverables:
  - [x] Workflow blueprint definition (intent/terminate-instance.ts)
  - [x] 6 node executors (workflow/nodes/validate-instance, drain-allocations,
        drain-ssh-sessions, cleanup-features, terminate-provider, cleanup-records)
  - [x] Compensation actions for reversible nodes
  - [x] Unit tests for each node
  - [x] Integration test: full terminate lifecycle on mock provider

## #LIFE-08: terminate-instance API + CLI wiring                   [Md] [DONE]
  Depends: #LIFE-07
  ---
  POST /v1/intents/terminate-instance endpoint.
  `repl instance terminate <id>` CLI command.

  Deliverables:
  - [x] API endpoint with TypeBox validation (routes.ts)
  - [x] CLI command (commands/instance.ts + client.ts)
  - [x] E2E test: terminate via CLI

## #LIFE-09: cleanup-manifest intent (blueprint + nodes)           [Xl] [DONE]
  Carries: BT3, BT4
  Spec: Ch07 §7.4
  Pseudo: intent/cleanup-manifest.ts.l1+l2.txt, cleanup.ts.l1.txt, related nodes
  Depends: #LIFE-06, #WF-03
  ---
  4-node DAG: load-manifest-resources → sort-and-group →
  cleanup-resources → delete-manifest.

  Deliverables:
  - [x] Workflow blueprint with priority-ordered cleanup (intent/cleanup-manifest.ts)
  - [x] Node executors (workflow/nodes/load-manifest-resources, sort-and-group,
        cleanup-resources, delete-manifest)
  - [x] Cross-manifest resource claiming (via resource/manifest.ts)
  - [x] Integration test: manifest cleanup releases all owned resources

## #LIFE-10: cleanup-manifest background trigger                   [Md] [DONE]
  Depends: #LIFE-09, #LIFE-04
  ---
  Wire manifest expiry scanner to automatically trigger cleanup-manifest.

  Deliverables:
  - [x] Connect manifestCleanupCheck → cleanup-manifest intent (main.ts)
  - [x] Integration test: expired manifest triggers cleanup

## #LIFE-11: Allocation reconciliation                             [Lg] [DONE]
  Spec: Ch05 §5.5
  Pseudo: control/src/background/reconciliation.ts.l1.txt
  Depends: #LIFE-01, #LIFE-02
  ---
  6 reconciliation tasks per spec: heartbeat timeout, warm pool health,
  orphaned claims, stalled transitions, provider state sync, allocation aging.

  Deliverables:
  - [x] Reconciliation runner with 6 task types (background/reconciliation.ts)
  - [x] Integration test for each reconciliation scenario (18 tests)

## #LIFE-12: Resource claiming protocol                            [Lg] [DONE]
  Spec: Ch04 §4.2
  Pseudo: control/src/resource/manifest.ts.l1.txt
  Depends: #LIFE-01
  ---
  Manifest-resource ownership with claim/release semantics.

  Deliverables:
  - [x] Implement claim/release in manifest resource layer (resource/manifest.ts)
  - [x] Cross-manifest resource transfer (claimResourceAtomic, parentClaimSubworkflowResource)
  - [x] Unit tests for ownership invariants (28 tests)

## #LIFE-13: Enforce resource layer boundary                       [Lg] [PENDING]
  Depends: #LIFE-01
  ---
  #LIFE-01 built resource/{allocation,instance,run}.ts but no callers use them.
  13 files still import directly from material/db, bypassing the resource layer
  and breaking the manifest → resource → material hierarchy.

  Callers to rewire (by directory):

  workflow/nodes/ (8 files — highest priority, these ARE the business logic):
    spawn-instance.ts    createInstance, updateInstance, getInstance
    claim-allocation.ts  getAllocation
    create-allocation.ts createAllocation, getAllocation, addResourceToManifest
    resolve-instance.ts  queryOne, findWarmAllocation
    finalize.ts          updateRun, getManifest, getInstance, getAllocation,
                         createAllocation, countInstanceAllocations
    wait-for-boot.ts     updateInstance, getInstance
    wait-completion.ts   updateRun
    start-run.ts         updateRun

  intent/ (1 file):
    launch-run.ts        createRun, getWorkflow

  api/ (3 files):
    routes.ts            various db queries
    agent.ts             various db queries
    middleware/auth.ts   getInstance

  Excluded from rewiring (intentionally stay on material/db):
    workflow/state-transitions.ts — IS the atomic transition layer, peer to resource
    workflow/engine.ts — workflow infrastructure, manages workflow/node lifecycle
    workflow/compensation.ts — workflow infrastructure
    workflow/patterns.ts — workflow infrastructure
    api/middleware/idempotency.ts — cross-cutting concern, raw query is appropriate

  Deliverables:
  - [ ] Rewire 8 node executor files to import from resource/ not material/db
  - [ ] Rewire launch-run.ts intent to use resource layer
  - [ ] Rewire API routes + agent.ts to use resource layer
  - [ ] Add resource/manifest.ts if needed (or defer to #LIFE-12)
  - [ ] Add lint rule or CLAUDE.md convention: node executors MUST NOT import
        from material/db (resource/ only)
  - [ ] No new tests needed — existing tests validate behavior is unchanged

  Note: #LIFE-07 and #LIFE-09 (new node executors) should use resource/ imports
  from the start. This task can run in parallel with those if the convention is
  established first.

================================================================================
SCRATCHPAD
================================================================================

(Design notes, session logs, open questions)
